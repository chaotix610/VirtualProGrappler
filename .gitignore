# Dependencies
node_modules/

# Build output
dist/

# Asset authoring tree — ~267MB of Blender, Quaternius and Mixamo originals.
# Nothing here ships. The subset that does is committed under assets/runtime,
# assets/textures, assets/glb and assets/artwork, and is copied out of this
# tree by `npm run assets:promote`.
assets/source/

# Local env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Agent / AI tooling — local only, never published
CLAUDE.md
AGENT.md
AGENTS.md
.claude/
.agents/
.codex
docs/**/claude-*.md
# Working notes, session handoffs and anything else written for an agent
# rather than for the project. Kept out of the public repository.
claude/

# OS / editor noise
.DS_Store
Thumbs.db
.idea/
.vscode/
*.sw?
*.suo
